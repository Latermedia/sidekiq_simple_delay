# frozen_string_literal: true

RSpec.describe SidekiqSimpleDelay do
  xit 'has a version number' do
    expect(SidekiqSimpleDelay::VERSION).not_to be nil
  end

  xit 'does something useful' do
    expect(false).to eq(true)
  end
end
